import { AppSyncResolverHandler } from 'aws-lambda';

// Stub: full implementation in task 12.1
export const handler: AppSyncResolverHandler<Record<string, unknown>, unknown> = async (_event) => {
  throw new Error('Not implemented');
};
